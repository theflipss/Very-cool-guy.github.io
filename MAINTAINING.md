# do not change this file
as a maintainer, your should enforce rules in README.md. in addition:

- Any content additions (e.g. new pictures, subsites) are allowed
- Content moves are allowed as long as they make sense.
- Content deletion is forbidden unless the same person who added it wants to delete it.
- Any feature additions (e.g. barrel roll on click) should first be checked if they are intrusive or makes the site unusable.
- Any feature changes that are unclear should be asked how they interact with the current feature.
- Do not clutter the main site. if it is too full with images, distribute them to subsites.
- Do not accept uploads of extremely huge codebases because they can be changed easily, instead ask for a hotlink via `.gitmodule`.
- Do not accept big changes that changes nothing visible, e.g. hundred line comments, a random part of microsoft source code that does nothing. Refactoring is allowed.
- Make sure there's no harmful (hateful or malware) content and audit rigorously.
